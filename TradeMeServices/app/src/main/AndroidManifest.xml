<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trademeservices.app" >

    <!-- used for Android Query plugin -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >
        <activity
            android:name="com.trademeservices.app.MainActivity"
            android:label="Search" >

        </activity>
        <activity
            android:name="com.trademeservices.app.InitLoad"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Trade Me Services"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.trademeservices.app.SearchResults"
            android:label="Results" >
        </activity>
        <activity
            android:name="com.trademeservices.app.Listing"
            android:label="Listing" >
        </activity>
    </application>

</manifest>
